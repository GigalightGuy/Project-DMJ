﻿
//static const int numThreads = 8;

RWStructuredBuffer<float4> points;

int nPointsXAxis;
int nPointsYAxis;
int nPointsZAxis;


float3 boundsSize;
float3 centre;
float3 offset;
float spacing;
float3 worldSize;

int indexFromCoord(uint x, uint y, uint z)
{
    return z * nPointsYAxis * nPointsXAxis + y * nPointsXAxis + x;
}